# TODO

## Навигация
- [x] Реализовать кнопку "Up" навигации в `ToDoActivity`, `HistoryActivity`, `GameDetailActivity` (добавить `NavigationIcon` с `onBackPressed()` в `Scaffold` topBar)

## Валидация
- [x] Добавить валидацию даты с обратной связью пользователю в `PlayResultDialog` — при некорректном формате показывать ошибку через `supportingText` в `OutlinedTextField` вместо молчаливого fallback на `now`

## Тесты
- [x] Покрыть тестами ViewModel (`ToDoViewModel`, `HistoryViewModel`, `GameDetailViewModel`)
- [x] Покрыть тестами Repository (`GameRepository`, `PlayResultRepository`)
- [x] Покрыть тестами DAO (`GameDao`, `PlayResultDao`)

## Разное от человека
- [ ] При обновлении версий не сохраняются данные (как будто бы). Надо проверить этот момент и гарантировать сохранение данных в случае миграций (обновлений).
- [ ] В списке History давай для каждой строчки списка выводим не только название игры, но и мелким шрифтом кол-во сыгранных партий и дату и время последней партии, а также среднее значение длительности партии.
- [ ] В списке History предусмотреть переключение сортировки по названию, по дате и времени последней партии, по количеству партий. по умолчанию сортировка по имени. кнопками над списком предусмотреть возможность переключения.
- [ ] В главном окне под имеющимися кнопками добавить ещё одну "Choose 1st player". По нажатию на эту кнопку должен открываться новый экран, ожидающий прикосновений пальцев (multi-touch). Как только к экрану прикоснулись 2 или больше пальцев он ожидает 3 секунды дополнительных касаний, после чего из зафиксированных касаний выбирает одно случайным образом. И обязательно визуально указывает на выбранное касание. Это необходимо, чтобы выбрать первого игрока в начале настольной игры. Все игроки касаются пальцем и приложение выберет одно из касаний.
